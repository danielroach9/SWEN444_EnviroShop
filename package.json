{
  "name": "enviro-shop",
  "private": true,
  "version": "0.0.0",
  "description": "Making the world healthier a bite at a time.",
  "devDependencies": {
    "http-server": "^0.9.0"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server ./client -a localhost -p 8000 -c-1"
  }
}
